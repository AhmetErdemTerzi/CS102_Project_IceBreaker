<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.icebreak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IceBreak">
        <activity android:name=".ScoreBoardActivity"/>
        <activity android:name=".OutdoorEventMainActivity"/>
        <activity android:name=".LobbyActivity"></activity>
        <activity android:name=".TaskGiverActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".TaskReceiverActivity" />
        <activity android:name=".OutdoorScoreboardActivity" />
        <activity android:name=".GlobalTask2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".GlobalTask1"
            android:label="@string/title_activity_global_task1" />
        <activity android:name=".reviewSuggestionsActivity" />
        <activity android:name=".notifActivity" />
        <activity android:name=".playTabActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UserTab" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />

</manifest>